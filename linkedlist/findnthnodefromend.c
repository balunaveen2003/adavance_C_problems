#include<stdio.h>
#include<stdlib.h>

struct node {
	int data;
	struct node *next;
};
struct node *head=NULL;

void creatanode(int d);
void Nthnodefromend();
void display();

void creatanode()
{
	struct node *new,*trav;
	new=malloc(sizeof(struct nod))
}

